<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:contentDescription="Picture of a traffic light in the current application state"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".MainActivity">


    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/redLightImage"
            android:layout_width="368dp"
            android:layout_height="567dp"
            android:visibility="visible"
            app:srcCompat="@drawable/stoplight_red" />

        <ImageView
            android:id="@+id/yellowLightImage"
            android:layout_width="368dp"
            android:layout_height="567dp"
            android:visibility="invisible"
            app:srcCompat="@drawable/stoplight_yellow" />

        <ImageView
            android:id="@+id/greenLightImage"
            android:layout_width="368dp"
            android:layout_height="567dp"
            android:visibility="invisible"
            app:srcCompat="@drawable/stoplight_green" />

    </FrameLayout>

    <Button
        android:id="@+id/switchButton"
        android:layout_width="196dp"
        android:layout_height="48dp"
        android:layout_marginTop="48dp"
        android:backgroundTint="#FF0000"
        android:text="@string/button_stop"
        />

</LinearLayout>